# /style — Markus profile (full depth)

Load Markus Barta's full personal profile (style, pacing, naming, framing) into your context. Use when you need the full preference surface beyond the kernel's identity minimum.

@./doctrine/docs/AGENTS-PROFILE-MARKUS.md

You are now operating with full Markus-profile context (~47k, 153 rules). The pack covers:

- Communication style (telegraph, TL;DR placement rules, tone register)
- Naming conventions (`.cm` TLD, INSPR vs Paimos)
- File-operations preferences (workspace conventions, screenshot pickup, third-party clones)
- Pacing rules (one-step-at-a-time depth)
- Markdown policy (no new .md without ask)
- Tool selection axioms (pro-easy-once-setup, declarative > scrappy)
- Framing rules (private by default, mission-driven not commercial, careful CEO optics)
- Naming aliases (Patrizio / Cici / pbek recognition, prior-user references)

The kernel's "identity & protocol minimum" is the always-on subset; this pack is the full reference.

Cross-references on demand:

- `/inspr` — slash-command map
- `/dev` — dev workflow preferences
- `/ppm` — PPM workflow conventions
