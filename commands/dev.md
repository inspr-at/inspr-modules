# /dev — DEV domain context

Load the DEV domain pack into your context for code work, refactor, tests, git workflow depth, build/test gates, and dev-tooling patterns.

@./doctrine/docs/AGENTS-DOMAIN-DEV.md
@./doctrine/docs/AGENTS-AGENT-DEV.md

You are now operating with full DEV-domain context. The pack covers:

- Git workflow depth (auth, identity, safety techniques beyond kernel basics)
- Build / test / pre-commit gates
- Code style + refactor preferences
- Tooling patterns (`gh`, `just`, script design, shell quoting)
- Markdown policy + file-operations conventions

Cross-references on demand:

- `/secrets` — secrets handling (agenix, env files, 1P CLI)
- `/nix` — nix-darwin, Home Manager, NixOS modules
- `/ops` — fleet ops, SSH, deploys
- `/ppm` — ticket workflow

If exhaustive citation needed, see `inspr-modules/docs/AGENTS-CORE.md` (full universal-rules reference, ~64k).
